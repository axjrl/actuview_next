import React from 'react';

import { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Videos :: actuview',
    description: '...',
}
const Videos = () => {
    return (
        <div>
            videos
        </div>
    );
};

export default Videos;