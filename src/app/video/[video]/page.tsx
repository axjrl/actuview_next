
interface Props {
    params: {
        video: string
    }
}
const VideoPage = ({params}:Props) => {
    console.log(params)
    return (
        <div>

        </div>
    );
};

export default VideoPage;